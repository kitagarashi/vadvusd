workflows:
  ios-test:
    name: iOS Test Build
    environment:
      xcode: latest
      cocoapods: default
    scripts:
      - name: Build app
        script: |
          xcodebuild build -workspace vadvusd.xcworkspace -scheme vadvusd